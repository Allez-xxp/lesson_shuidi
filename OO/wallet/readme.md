# 大厂大型（核心）项目，如何设计用面向对象
- uuid (npm i uuid)
 v1方法，代表用户的唯一串码，字符串
 
- 区块链，钱在区块链里就是一个不会被篡改的字符串
 概念:去中心化

- 私有属性不可改
 private
 在js中，_id(不可改)
 私有属性，提供一个get方法来访问

- 设计一下balance（金额）的修改
 进钱，出钱
 js 使用——attribute 这种做法是是一种约定，private
 一般情况下，可以这么写。
 getID,increateBalance(),内部修改这些属性private。

- 真正的私有，到底怎么做？
 闭包创建私有类，（红宝书里的内容） 
 