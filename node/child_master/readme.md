- 你的电脑是几核？
  node index.js 单进程语言
  发挥了一个CPU的作用
  - 进程 process
    应用 appendChild()
    操作系统挂载运行程序的单元
    进程拥有独立的资源 如内存 绝对安全

-线程 thread，运算调度的单元，属于进程。（计算都是在线程上的）
js本质上没有多线程能力的

-核心：child_process 子进程模块
 现代编程，基于硬件
 启动子进程，子进程会寻找另外的CPU运行
 子进程数小于等于CPU核数；效率更高；类似消息机制